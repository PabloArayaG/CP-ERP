# ====================================
# VARIABLES DE ENTORNO - BACKEND
# ====================================

# Puerto del servidor (Render lo asigna automáticamente)
PORT=5000

# Entorno de ejecución
NODE_ENV=production

# URL del frontend (para CORS)
# En desarrollo: http://localhost:3000
# En producción: https://tu-app.vercel.app
FRONTEND_URL=https://tu-frontend.vercel.app

# Base de datos (si usas una)
# DATABASE_URL=postgresql://usuario:password@host:puerto/database

# Claves de API (si las necesitas)
# API_KEY=tu_clave_secreta

# JWT Secret (si usas autenticación)
# JWT_SECRET=tu_clave_jwt_super_secreta

# Firebase Admin (si usas Firebase Admin SDK)
# FIREBASE_ADMIN_KEY=tu_clave_admin_firebase 